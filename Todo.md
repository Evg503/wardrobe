# Todo: технологический стек проекта

## Построение плана квартиры

- [ ] Определить целевые платформы (iOS / Android / обе)
- [ ] iOS с LiDAR → использовать **Apple RoomPlan** (бесплатно, лучший результат)
- [ ] Android → исследовать **ARCore Depth API** + кастомная обработка point cloud
- [ ] Кроссплатформа → рассмотреть **Unity AR Foundation**
- [ ] Без разработки логики сканирования → оценить **Matterport API** или **Polycam API**

## Распознавание предметов по камере

- [ ] iOS → интегрировать **Apple Vision + Core ML**
- [ ] Android + iOS → рассмотреть **Google ML Kit** (быстрый старт) или **TFLite** (кастомные модели)
- [ ] Выбрать модель: **YOLO** (точность), **EfficientDet-Lite** (баланс), **MobileNet-SSD** (размер)
- [ ] React Native → `react-native-vision-camera` + `react-native-fast-tflite`
- [ ] Flutter → `google_mlkit_object_detection` или `tflite_flutter`
- [ ] Кастомный датасет → обучить через **Roboflow**, экспортировать в TFLite/CoreML

## Общие решения

- [ ] Определить, нужна ли работа офлайн (влияет на выбор: on-device vs cloud API)
- [ ] Если нужна высокая точность без разработки ML — оценить облачные API (AWS Rekognition, Google Cloud Vision)
- [ ] Проверить наличие LiDAR на целевых устройствах пользователей

## Архитектурное решение: одно приложение или два

**Решение: одно кроссплатформенное приложение на Flutter**

Контекст: solo-разработчик, оба функционала (план + распознавание), минимум бюджета.

- [x] Отказаться от двух параллельных нативных приложений — удвоение кода, багов, поддержки
- [ ] Выбрать **Flutter** как основной фреймворк
  - План квартиры: `arkit_plugin` (iOS/LiDAR) + `arcore_flutter_plugin` (Android)
  - Распознавание объектов: `google_mlkit_object_detection` (iOS + Android)
  - UI, бизнес-логика, сеть — один кодовый репозиторий на Dart
- [ ] Альтернатива: начать только с **нативного iOS** (Swift + RoomPlan + Core ML), если аудитория преимущественно iOS, Android добавить позже
- [ ] Учесть ограничение: на Android без LiDAR качество плана заметно хуже, чем на iOS с RoomPlan — это ограничение технологии, не фреймворка
