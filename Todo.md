# Wardrobe — Todo

## Выполнено

### Архитектура и стек
- [x] Выбрать Flutter как основной фреймворк (iOS + Android, один кодовый репозиторий)
- [x] Material 3 дизайн-система
- [x] Глобальное состояние через `InheritedNotifier` (без сторонних пакетов)
- [x] Персистентность через `shared_preferences` (счётчики, история сессий)

### AR-сканирование плана
- [x] ARKit (iOS) — детектирование горизонтальных и вертикальных плоскостей, визуализация оверлеями
- [x] ARCore (Android) — нативный platform channel (`GLSurfaceView` + `EventChannel` + `MethodChannel`)
- [x] Схематичный план через `CustomPainter` по данным сканирования
- [x] Сохранение результатов сканирования в историю

### Распознавание предметов
- [x] Google ML Kit Object Detection (встроенная base-модель)
- [x] EfficientDet-Lite0 TFLite (COCO 80 классов) — кастомная модель для мебели
- [x] Переключатель моделей base / EfficientDet в UI без перезапуска
- [x] Bounding box оверлеи поверх видеопотока
- [x] Исправлен баг конвертации YUV_420_888 → NV21 на Android

### Экспорт
- [x] Экспорт плана в PNG (через `RepaintBoundary`)
- [x] Экспорт плана в PDF (векторный, через пакет `pdf`)
- [x] Шаринг файлов через системный диалог (`share_plus`)

### UX
- [x] Онбординг — 4 слайда, показывается только при первом запуске
- [x] История сессий (AR-сканирование + распознавание) с `ExpansionTile`
- [x] Версия приложения в AppBar + About-диалог (`package_info_plus`)

### Инфраструктура
- [x] README.md с инструкциями сборки для macOS, Windows, Linux
- [x] Log.md — подробная история разработки с описанием багов и фиксов
- [x] Версионирование: `major.minor.patch+buildNumber`, поднимается с каждым билдом
- [x] Ветка `releases` с APK и iOS zip-архивами

---

## В работе

- [ ] Проверка и тестирование ARCore на реальном Android-устройстве
- [ ] Проверка и тестирование EfficientDet-Lite0 на реальных объектах

---

## Следующие шаги

### Качество распознавания
- [ ] Оценить точность EfficientDet-Lite0 на реальных объектах интерьера
- [ ] Рассмотреть дообучение модели на датасете мебели через Roboflow (при необходимости)
- [ ] Фильтрация дублирующихся объектов в потоке (один предмет — одна карточка)

### План квартиры
- [ ] Улучшить `CustomPainter` — более реалистичная схема с учётом реальных координат плоскостей
- [ ] Добавить масштабную линейку на схему плана
- [ ] Сохранение плана в галерею устройства (не только временную папку)

### UX и полировка
- [ ] Локализация — вынести все строки в `AppLocalizations`
- [ ] Тёмная тема
- [ ] Haptic feedback при обнаружении объекта/плоскости
- [ ] Splash screen с иконкой приложения

### Технический долг
- [ ] Обновить `camera_android_camerax` и `share_plus` до версий с Built-in Kotlin (убрать KGP warning)
- [ ] Обновить `google_mlkit_object_detection` до версии с поддержкой Swift Package Manager
- [ ] Добавить unit-тесты для `ExportService` и `ArScanService`
- [ ] Git LFS для APK-артефактов в ветке `releases`
